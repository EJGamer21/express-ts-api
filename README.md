# API made to serve an ERP built with VueJS

## Steps to run this project:

1. Run `npm i` command.

2. Setup database settings inside `./src/config/db.config.ts` with `./src/config/db.config.example.ts` file.

3. Run `npm start` command.